# Standalone build dirs (own target dir, never the shared ./target).
/target
/target-pd
# The ELF closure build artifacts (regenerated by scripts/cross-compile-closure.sh;
# libdregg_lean_elf.a is 58.9 MB — a build output, not source).
/out
Cargo.lock
