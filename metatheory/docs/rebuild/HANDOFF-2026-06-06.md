# dregg2 — Circuit / ZK waves handoff (2026-06-06)

Entry point for the next agent continuing **whole-turn ZK**, **emitted→spec diamonds**, and **no-lurking-holes** policy.

> **First move:** `cd /Users/ember/dev/breadstuffs/metatheory && lake build Dregg2`  
> **Expected today:** **RED** — `Dregg2.Circuit.TurnEmit` fails to compile (see §2).  
> **Green submodule:** `lake build Dregg2.Circuit.EffectEmittedRefinement` (~3131 jobs).

Session transcript (detail dropped from summary):  
`/Users/ember/.grok/sessions/%2FUsers%2Fember%2Fdev%2Fbreadstuffs%2F019e9c15-cf6c-7df3-8614-22f0722e78d7%2Fupdates.jsonl`

---

## 0. Non-negotiable policy (this arc)

1. **Semantics-first** — Rust/FFI must align with verified Lean (`execFullTurnA`, `execFullA`, forest/joint paths).
2. **No silent holes** — forbidden: `unknownAirName` pretending to be emission, `exact h` spec-fallback in circuit dispatch, commented-out emission. **Explicit `sorry` + named portal is required.**
3. **Whole-turn ZK + inter-vat** — `CONTRIBUTING.md` circuit restrictions are **not** binding for this work.
4. **Poseidon2 must be efficient in-circuit** (Wave 4) — reuse `merkle_hash` wire forms, not abstract `ℤ` injectivity portals.

---

## 1. What landed (verified / built this session)

### Wave 1 — turn stack (prior + stable)

| Module | Role |
|--------|------|
| `Circuit/Spec/Turn.lean` | `turnSpec`, `execFullTurnA_iff_turnSpec` |
| `Circuit/ActionDispatch.lean` | 56/56 `fullActionStep` + executor bridges |
| `Circuit/TurnRefinement.lean`, `TurnWitness.lean` | turn chain + witness scaffold |
| `Circuit/GadgetRefinement.lean` | digest / Merkle portal composition |

### Wave 2 — emission registry (prior + stable)

- **~31 `Circuit/Inst/*` files** — emission moved out of `/- doc -/` into real `def`/`theorem` sections.
- **`EffectEmitRegistry.lean`** — **53** emitted descriptors; `actionAirName` maps **56/56** arms (3 deferred → `*-HOLE` names, registry returns `none`).
- **`TurnEmit.lean`** — `defaultDescriptorLookup := effectEmitRegistry`.

### Big wave — batch emitted refinement (major progress)

- **`EffectEmittedRefinement.lean` §19 (batch-2)** — **~32/35** Inst effects now have **proved** `*_emitted_refines_spec` via Inst `*_full_sound` + generic `effect{1,2,dual,triple,quint}_emitted_refines_bespoke_spec`. **Builds green.**
- **Batch-1** (~18 effects: mint, burn, transfer, delegate, escrow, queue enqueue, exercise hold-gate, etc.) — proved earlier, still green.
- **`CodecRoundtrip/Wire.lean`** — `blockHeight` optional fix (unblocked full builds at prior checkpoint).
- **`CircuitOpenFronts.lean`** — named open-front inventory + Wave 4–7 `sorry` portals (tracked, not hidden).
- **`Poseidon2Emit.lean`** — efficient sponge descriptor reusing `merkle_hash` / `merkleConstraintsWire`; proved faithfulness to Merkle portal; **2 explicit sorry** for state-commit / log-hash sponge binding.
- **`DigestPortal.lean`** — bridge abstract digest portals toward Poseidon2 emit (explicit sorry for CR→injectivity).
- **`TurnAdmission.lean`**, **`TurnCircuitCompose.lean`**, **`Inst/ExerciseInnerTurn.lean`** — explicit sorry portals (Wave 5–7).

### Rust (prior checkpoint)

- Extra `lean_emitted` roundtrip tests; `cargo test --lib lean_emitted` was **10/10** at last reported green.

---

## 2. Current build status (2026-06-06)

| Target | Status | Notes |
|--------|--------|-------|
| `Dregg2.Circuit.EffectEmittedRefinement` | **GREEN** | 3 intentional `sorry` in batch-2 (see §3) |
| `Dregg2.Circuit.TurnEmit` | **RED** | compile errors — partial wiring of batch-2 proofs |
| `lake build Dregg2` | **RED** | fails on `TurnEmit` |

### TurnEmit compile failures (fix first)

1. **Variable shadowing in `stepEmittedEncodeAgrees`** (~lines 477–488): arms like `.exportSturdyRefA sw actor …` bind `sw : Nat`, so `sw.assignment` resolves to `Nat.assignment` (missing). **Rename** action arg (e.g. `swId`) or use `StepWitness` name from outer scope.

2. **`Function expected at … .mpr hcircuit`** (~lines 733, 739, 772, 799, 810, 821): several `*_emitted_equiv_circuit` lemmas return `↔` but call sites treat them like `↔` **functions** incorrectly, or equiv theorem is missing / wrong arity. Pattern that works elsewhere:

```lean
exact foo_emitted_refines_spec …
  ((foo_emitted_equiv_circuit … st args st').mpr hcircuit)
```

   Audit each batch-2 equiv theorem in `EffectEmittedRefinement.lean` and match **mint/burn** arms in `TurnEmit` exactly.

3. **unsealA** (~741): needs `box` in `UnsealArgs` — circuit step must thread `findSealedBox` (subagent started this; may be incomplete).

---

## 3. Explicit sorry inventory (circuit layer — intentional)

### `EffectEmittedRefinement.lean` (batch-2, 3 remaining)

| Theorem | Reason |
|---------|--------|
| `createCellFromFactoryA_emitted_refines_spec` | Inst proves `CreateFromFactoryCircuitSpec`, not full `CreateFromFactorySpec` — needs bridge lemma or change conclusion |
| `releaseCommittedEscrowA_emitted_refines_spec` | No Inst emission (`releaseCommittedEscrowAHoleName`) |
| `refundCommittedEscrowA_emitted_refines_spec` | No Inst emission (`refundCommittedEscrowAHoleName`) |

### `TurnEmit.lean` — `step_emitted_refines_fullActionStep` (4 remaining)

| Arm | Reason |
|-----|--------|
| `exerciseA` | hold-gate only; inner turn + R4 facet-mask not in circuit |
| `createObligationA` | no Inst / registry `none` |
| `releaseCommittedEscrowA` | no Inst |
| `refundCommittedEscrowA` | no Inst |

### `TurnEffectRefinement.lean` — `HolePortals` (4 remaining)

| Portal | Reason |
|--------|--------|
| `hole_exerciseA_circuit_refines_spec` | inner turn composite |
| `hole_createObligationA_circuit_refines_spec` | no Inst |
| `hole_releaseCommittedEscrowA_circuit_refines_spec` | no Inst |
| `hole_refundCommittedEscrowA_circuit_refines_spec` | no Inst |

### Wave 4–7 tracked portals (not regressions — named fronts)

- `Poseidon2Emit`: `hole_state_commit_sponge_binding`, `hole_log_hash_sponge_binding`
- `DigestPortal`: injectivity holes from Poseidon2 CR
- `TurnWitness`, `TurnAdmission`, `TurnCircuitCompose`, `CoordinatedTurnEmit`, `ExerciseInnerTurn`
- `CircuitOpenFronts`: `hole_poseidon2_digest_sound`, `hole_turn_circuit_fold_emitted`, `hole_coordinated_covenant_in_circuit`, `hole_exercise_inner_turn_in_circuit`

### Registry deferred (fail-closed, correct)

```lean
-- EffectEmitRegistry.lean
createObligationAHoleName      := "dregg-createObligationA-HOLE"
releaseCommittedEscrowAHoleName := "dregg-releaseCommittedEscrowA-HOLE"
refundCommittedEscrowAHoleName  := "dregg-refundCommittedEscrowA-HOLE"
```

`#guard` asserts `effectEmitRegistry` returns `none` for each.

---

## 4. Next steps (priority order)

### P0 — restore green build (~1–2h)

1. Fix `TurnEmit.lean` shadowing + `.mpr` mismatches (§2).
2. `lake build Dregg2` → green.
3. Commit with message noting TurnEmit wiring completion.

### P1 — close Wave 3 remaining holes (~1 day)

1. **Inst for 3 deferred actions** OR keep sorry but add `CircuitOpenFronts` entries only:
   - `createObligationA` — new Inst or alias `createEscrowE`?
   - `releaseCommittedEscrowA` / `refundCommittedEscrowA` — alias `releaseEscrowE` / `refundEscrowE` with `CommittedEscrowSettleSpec` + `releaseSettleAuthB` / `refundSettleAuthB`
2. **`createCellFromFactoryA`** — prove `CreateFromFactoryCircuitSpec → CreateFromFactorySpec` or retarget emitted conclusion to circuit spec and lift at `ActionDispatch`.
3. **`TurnEffectRefinement`** — replace remaining `hole_*` with real `fullActionCircuitStep` + `*_full_sound` (batch-2 Inst modules all have diamonds).
4. Update `CircuitOpenFronts.openFronts` — shrink list as holes close; run `#guard countOpenFronts`.

### P2 — Wave 4 efficient Poseidon2 (~2–3 days)

1. Wire `Poseidon2Emit` into `StateCommit` frame sponge (`CH`/`RH`/`LH`).
2. Range checks via `Lookup.lean`.
3. Re-emit descriptors + Rust goldens (`dregg-poseidon2-compress-v1` already pinned in `Poseidon2Emit`).
4. Close `DigestPortal` sorrys from Poseidon2 CR assumption (document as trust portal).

### P3 — Wave 5 whole-turn (~2 days)

1. `TurnCircuitCompose` — per-step fold without catch-all sorry.
2. `TurnAdmission` — macaroon caveat chain column.
3. Rust: multi-step shadow + registry loader; FFI turn parity (`turn/src/lean_shadow.rs`, `dregg-lean-ffi`).

### P4 — Waves 6–7 (~3+ days)

1. Coordinated covenant/charter in AIR (`CoordinatedTurnEmit` / `CoordinatedForestGLift`).
2. Exercise inner-turn witness + R4 facet-mask (`ExerciseInnerTurn.lean`).
3. Handler lemmas: `makeSovereignA`, `receiptArchiveA`, queue §6.6 honest-path when `actor ≠ cell`.

---

## 5. Key file map

```
metatheory/Dregg2/Circuit/
  EffectEmittedRefinement.lean   ← batch-1 + batch-2 emitted ⊑ spec (GREEN)
  EffectEmitRegistry.lean        ← 53 registry + 3 HOLE names
  TurnEmit.lean                  ← per-step emitted ⊑ fullActionStep (RED — fix here)
  TurnEffectRefinement.lean      ← fullActionCircuitStep + hole portals
  CircuitOpenFronts.lean         ← named gap registry
  Poseidon2Emit.lean             ← Wave 4 sponge gadget
  DigestPortal.lean              ← digest bridge
  TurnWitness.lean / TurnAdmission.lean / TurnCircuitCompose.lean
  Inst/*                         ← per-effect v1/v2 Inst + emission + *_full_sound
  Spec/*                         ← independent declarative specs
  ActionDispatch.lean            ← fullActionStep / turnSpec / exec bridges

metatheory/Dregg2/Exec/
  HandlerExecutor.lean           ← handler refinement lemmas
  CodecRoundtrip/Wire.lean       ← wire roundtrip

Rust:
  turn/src/lean_shadow.rs
  dregg-lean-ffi/
  circuit/src/ (descriptor decoder)
```

---

## 6. Proof patterns (copy-paste for next agent)

### effect2 emitted ⊑ spec (v2 Inst)

```lean
theorem fooA_emitted_refines_spec … (h : fooAEmittedStep …) : FooSpec … :=
  effect2_emitted_refines_bespoke_spec S (fooE …) fooAAirName
    (fun pre args post => satisfiedE2 S (fooE …) (encodeE2 S (fooE …) pre args post))
    (fun pre args post => FooSpec …)
    (fun pre args post hc => fooA_full_sound S … hc)
    (fun pre args post => fooA_emitted_equiv_circuit S … pre args post)
    s args s' h
```

### effect1 emitted ⊑ spec (v1 CommitSurface)

```lean
theorem barA_emitted_refines_spec … (hwf hwf' …) (h : barAEmittedStep …) : BarSpec … :=
  effect1_emitted_refines_bespoke_spec S barE barAAirName
    (fun pre args post => BarSpec …)
    (fun pre args post hc => barA_full_sound S … hwf hwf' hc)
    pre args post h
```

### TurnEmit arm (after equiv exists)

```lean
| .fooA … =>
    simp only [fullActionStep]
    exact fooA_emitted_refines_spec S … st ⟨…⟩ st'
      ((fooA_emitted_equiv_circuit S … st ⟨…⟩ st').mpr hcircuit)
```

---

## 7. Agent / parallelization notes

- **Do not** spawn wide parallel agents on `TurnEmit` + `TurnEffectRefinement` simultaneously — this session's subagents **spun** and left `TurnEmit` in a broken partial-wiring state.
- **Safe parallel split** after P0 green:
  - Track A: 3 deferred Inst effects + factory spec bridge
  - Track B: Poseidon2 + DigestPortal
  - Track C: Turn compose + admission
  - Track D: Coordinated + exercise inner turn
- Always run `lake build Dregg2` as orchestrator gate before declaring a wave done.

---

## 8. Success criteria for “waves done”

- [ ] `lake build Dregg2` green
- [ ] `step_emitted_refines_fullActionStep` — 0 sorry except 4 named deferrals (exercise inner, 3 HOLE actions)
- [ ] `EffectEmittedRefinement` batch-2 — 0 sorry except 3 named deferrals (or factory bridge proved)
- [ ] `TurnEffectRefinement` — 0 `hole_*` sorry except same 4 deferrals
- [ ] `CircuitOpenFronts.openFronts` — Wave 3 diamond entries removed; count updated
- [ ] Poseidon2 frame/log sponges — proved or explicit sorry only in `Poseidon2Emit` / `DigestPortal`
- [ ] Rust `lean_emitted` + turn FFI parity tests green

---

*Handoff written at end of Composer-2.5 session — circuit/ZK parallel waves. Thank you for an excellent push on semantics-first, fail-closed proof integrity.*