# Handoff — 2026-06-03 (the intent-as-co-receipt / metatheory-of-web3 arc)

Entry point for the next agent. Read this first, then the docs it points to. The work is at a **clean,
green, committed resting point** (commit `6ed3df7a`, full library `Build completed successfully (3500
jobs)`, 0 errors / 0 sorry / 0 axiom-hygiene FAIL).

---

## 0. Orientation — read in this order

1. **`INTENT-AS-CO-RECEIPT.md`** — the design spine (the *why* and the *what*). §0 = constructive
   knowledge is the constraint (the standing frame: "to know X is to hold a witness for X"; the
   validity test *"what is the witness, and whose construction is it?"*). §1–7 = the turn's two faces
   (receipt ⊣ intent), the four-faced intent, the `∫^B` solver, causal/frame time, DeFi.
2. **`EFFECT-FIDELITY-LEDGER.md`** — dregg1 `apply.rs` vs Lean `execFullA`, per-effect verdicts. What
   the apps can trust today vs the named gaps.
3. **`INTENT-REFS-{resources,optics,time,web3,linear}.md`** — the 5 reference pillars (papers in
   `pdfs/`), each ending with a "what to formalize first + which mathlib pieces to reuse" recommendation.
4. **`PHASE-2-INTENT-SPEC.md`** — the concrete next build (the `Intent` structure, layered option (c)).
5. The standing project memory: `~/.claude/projects/-Users-ember-dev-breadstuffs/memory/` (esp.
   `project-2026-06-02-ambition-program.md`, `project-dregg2-coverage-map.md`).

---

## 1. The big picture (what this arc is)

dregg2 = a Lean4 verified metatheory. This arc is building it toward **a first-class metatheory of
web3**: the turn has two faces — **receipt** (it happened) and **intent** (let it happen) — adjoint
through `Predicate ⊣ Witness` (`Dregg2.Laws`), funded by **userspace escrow**, time-typed as
**causal-or-frame**, conserved by the kernel. Anoma's resource machine is the nearest neighbor; **our
edge is entirely formal** (machine-checked conservation/solver-soundness + the receipt⊣intent
adjunction Anoma lacks + relativistic time-typing + proven userspace-escrow).

Everything is read off the constructive-knowledge constraint, which is why independent efforts (Anoma,
ours) converge — the constraints do the choosing.

---

## 2. The 4-phase plan (ember-approved) — where we are

**Phase 1 — the relativistic time foundation. ✅ DONE (commit `6ed3df7a`).**
`Dregg2/Time/{Causal,Frame,Deadline}.lean`, green + wired into `Dregg2.lean`.
- `Causal.causalAfter (B E now)` = `E ≺ now` over the lace partial order (`Blocklace.precedes`):
  monotone + frame-invariant (no oracle) + NON-total (a Byzantine fork is concurrent — real teeth).
  The lightcone-fact deadline; carries NO §8 carrier.
- `Frame.frameWithin (F T δ)` = a `TimeAuthority`'s attested wall-clock frame (a `Temporal`
  `WitnessedPredicate`): sound via `registry_sound`, FALSE without the authority (not true-for-free),
  δ carried first-class. `FrameHonesty` = the explicit §8 skew carrier (proved non-trivial, never `True`).
- `Deadline` sum type FORCES the distinction (`causalAfter_no_frame_dependency` = `Iff.rfl` vs
  `frameWithin_has_frame_dependency` flips across registries). **`commit_wait_bridge`** (the keystone):
  `frame ∧ waited(2δ) ∧ honesty ⟹ causal` (Spanner external consistency — "wait out the skew,
  frontrunning is causally excluded"), with `commit_wait_needs_full_2delta` proving the 2δ load-bearing.

**Phase 2 — the `Intent` core. ← NEXT.** See `PHASE-2-INTENT-SPEC.md`. Decision locked: **layered
option (c)** — `Intent` abstract over a `[ResourceTheory R]` (the convertibility preorder = the match
relation, ~free from the resources study) + a concrete `KernelIntent` instantiation the auction proves
on. `Match` = mathlib's coend directly (`CategoryTheory.Limits.Types.coend`, it ALREADY exists — see
the optics study). Receipt⊣intent adjunction proved **incrementally** (define `Intent`+`fulfill`, prove
unit/counit laws as we go, not front-loaded).

**Phase 3 — conservation-as-monoid-hom + userspace escrow.** Restate per-asset conservation as an
`AddMonoidHom` invariant (mathlib reuse; the linear study). Build the **userspace-escrow cell-program**
and prove it **≥ kernel-escrow** (ember's hard bar — see §4). Safety becomes corollaries of the global
kernel invariants (conservation + slot-caveat permanence), which is *stronger*, not weaker.

**Phase 4 — the gallery sealed-bid auction**, on all of it. Prove: (a) userspace-escrow ≥ kernel-escrow,
(b) causal reveal-ordering excludes frontrunning, (c) conservation across the settle. Benchmarks vs
Penumbra (a proof, not a DKG trust assumption). The auction is an intent-matching system; this proof
validates the whole stack.

---

## 3. Locked design decisions (do NOT relitigate without ember)

- **Escrow → userspace**, but ONLY after proving userspace-escrow ≥ kernel-escrow (Phase 3/4). Until
  that proof lands, **kernel escrow STAYS** (no safety lost in the interim). This SUPERSEDES fixing the
  committed-escrow kernel shadows (ledger §queue-escrow) — they're going userspace, not being patched.
- **Time = causal vs frame, typed** (DONE). NOT a global `Nat` clock, NOT "blocklace depth baked into
  the metatheory" (that would collapse the consensus-symmetry). A "time authority" is a credential
  issuer whose attestation stream carries monotonicity + liveness + skew. **Beacon abstraction DROPPED**
  — time reuses `Predicate`/`Credential`/discharge, no new portal.
- **Intent = layered (c)** (abstract over a resource category + concrete kernel instance).
- **Match = mathlib coend** (don't roll our own).
- **The `∫^B` multi-hop solver is first-class from the start** (ember: makes it an *exchange*, not a
  bilateral matcher).
- **Constructive knowledge is the organizing frame** (spine §0): the witness-test gates every feature.

---

## 4. Hard-won truths + open gaps (carry these)

**Effect fidelity (the wave-1..4 work, `FullActionA` 46→56, faithful to dregg1).** GENUINE de-shadows
(I verified): `exerciseA` recurses inner effects (was a no-op); slot-caveats are wired into `setFieldA`
→ `stateStepGuarded` (Immutable/MonotonicSequence/WriteOnce reject); seal/unseal ACTUALLY move a
capability; lifecycle/factory real. **Open gaps from the fidelity ledger (NOT shadows, but named):**
- `exerciseA` gate is connectivity-only — facet-mask/`allowed_effects`, cap-expiry, revocation-channel
  NOT executable. ember chose "close these with the clock work" (the auth-gap question) — so the
  intent/escrow phases should thread the identity/facet gates as they go.
- `fulfillObligationA`/`slashObligationA` thread `actor` into NO gate (anyone can fulfill/slash) — but
  these are obligation-lifecycle effects likely subsumed by userspace escrow + the Deadline types now.
- `releaseCommittedEscrowA`/`refundCommittedEscrowA` are SILENT shadows (aliased to plain escrow) —
  but committed escrow is going userspace (see §3), so these die rather than get patched.

**Discipline (NON-NEGOTIABLE — ember has corrected on these):**
- **OPUS ONLY for feature/proof development.** NEVER sonnet for features. Sonnet only for mechanical
  comment cleanup or simple Explore reference-enumeration. (ember was emphatic.)
- **Read the ground truth YOURSELF** — don't delegate *understanding*. Use read-only agents only for
  fan-out search / reference-gathering, not for the core "what does this code mean" judgment.
- **NEVER `git checkout`/reset/stash to discard WIP.** If a workflow leaves a broken tree, WIP-commit
  it (`git commit -m "WIP [BROKEN BUILD]: ..."`) to preserve, then fix-forward. NEVER revert.
- **Gate the FULL build yourself in the main loop before committing** any agent/workflow output. A
  green standalone module ≠ green library. `#assert_axioms`-clean ≠ faithful/non-vacuous — READ the
  load-bearing bodies (verify-don't-trust).
- **Bring model-shape calls to ember.** A wrong model is worse than none (we'd be committed to it).
- **Build discipline:** `(cd /Users/ember/dev/breadstuffs/metatheory && ~/.elan/bin/lake build
  <Module>) > /tmp/x.log 2>&1; echo "EXIT=$?"` — capture the exit; a trailing `grep -c` can make the
  shell exit non-zero even on a clean build (read the log, not just `$?`).
- **Workflows that build features:** Opus draft + Opus verify-and-repair (adversarial
  de-vacuification — the verify pass repeatedly catches its own vacuities). Each builds its file green;
  the main loop does the final full-build gate + single commit. Sequential when the shared dispatch is
  touched; parallel only for import-disjoint files.
- 1Password may reject signing when ember is away → `--no-gpg-sign` is fine (flags away-work).

---

## 5. Substrate that already exists (the studies' headline: most of it is there)

- **mathlib coend** = `Limits.Types.coend F := Quot (coendRel F)` (v4.30, `Limits/Types/End.lean`) IS
  `Match(A,C) = ∫^B`. Profunctor composition is mathlib's own future-work (= the coend).
- **The lace** = `Blocklace.precedes (≺)` partial order (a causal set) — `causalAfter` rides it.
- **Predicate/Credential/discharge** = `Authority/{Predicate,Credential,ThirdPartyDischarge}` (the
  `Temporal` `WitnessedKind`, `registry_sound`) — `frameWithin` rides it. The existing
  `MAX_DISCHARGE_AGE=300` was literally a `frameWithin` missing its δ.
- **Iris cameras + frame rule seed** = `Resource.lean` + `Exec/MultiAsset.lean` (`Fpu`,
  `maTransfer_untouched`, `maExec_conserves_per_asset`).
- **MPST + one-shot linearity** = `Coordination.lean` + `Await.lean` (compare, don't rebuild).
- **The Hatchery** (`Verify/{Tactics,Frames,Contract,Catalog,Regression}`) — `carry_forever`,
  `exec_frame`, `CellContract`, the shape-catalog macros; the userspace-verification toolkit. The
  auction's carried-safety proofs should reuse it.

---

## 6. If you're the next Claude: your first move

`cd /Users/ember/dev/breadstuffs/metatheory && ~/.elan/bin/lake build Dregg2` → confirm green (3500
jobs). Read the docs in §0. Then start **Phase 2** per `PHASE-2-INTENT-SPEC.md` — Opus, gate the full
build yourself, bring the `Resource`/`Outcome` category-choice details to ember as they sharpen.
