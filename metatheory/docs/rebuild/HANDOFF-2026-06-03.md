# dregg2 — State & Master Roadmap (handoff 2026-06-03)

Entry point for the next agent. This is the WHOLE program, not one arc. The work is at a clean, green,
committed resting point: commit `6ed3df7a`, full library `Build completed successfully (3500 jobs)`,
0 errors / 0 sorry / 0 axiom-hygiene FAIL.

> First move: `cd /Users/ember/dev/breadstuffs/metatheory && ~/.elan/bin/lake build Dregg2` → confirm
> green (3500 jobs). Then read §0 reading list, then pick a track from §4.

---

## The vision — three nested goals

1. **A verified metatheory of the dregg execution laws** (l4v/seL4-shaped, in Lean4). Mostly built.
2. **→ a first-class metatheory of web3** — the turn as receipt ⊣ intent, funded by userspace escrow,
   time-typed causal-vs-frame, conserved by the kernel; DeFi as the application. The active frontier.
3. **Surpass dregg1 in EVERY implementation detail, and reach l4v-quality THROUGHOUT the stack** — the
   standing bar (ember). Includes privacy parity, the Rust cutover, and circuits.

The organizing frame is **constructive knowledge**: "to know X is to hold a witness for X." Every
feature passes the validity test *"what is the witness, and whose construction is it?"* — if it needs
ambient authority / unwitnessed existence / a global *now*, it's invalid or must be an explicit trust
portal. (`INTENT-AS-CO-RECEIPT.md` §0.)

---

## 0. Reading order

1. **This doc** (state + the full roadmap, §1–§5).
2. `INTENT-AS-CO-RECEIPT.md` — the metatheory-of-web3 spine (§0 constructive knowledge; receipt⊣intent;
   the four-faced intent; the `∫^B` solver; causal/frame time; DeFi).
3. `EFFECT-FIDELITY-LEDGER.md` — dregg1 `apply.rs` vs Lean `execFullA`, per-effect verdicts + open gaps.
4. `PHASE-2-INTENT-SPEC.md` — the concrete next build (the `Intent` structure, layered option (c)).
5. `INTENT-REFS-{resources,optics,time,web3,linear}.md` — the 5 reference pillars (papers in `pdfs/`).
6. `EXTERNAL-LEAN-REFERENCES.md` — adopt/avoid/build verdicts for external Lean libs (CTL/μ, IFC,
   coalgebra, crypto, liveness) — TCB-hygiene gated.
7. `HATCHERY.md` — the userspace-verification toolkit (built, H1–H4).
8. Project memory: `~/.claude/projects/-Users-ember-dev-breadstuffs/memory/`
   (`project-2026-06-02-ambition-program.md`, `project-dregg2-coverage-map.md`,
   `project-the-swap-roadmap.md`, `project-dregg4-vision.md`).

---

## 1. Where we are — what is VERIFIED today (green, committed)

- **The executable kernel** — `Exec/TurnExecutorFull.lean`: `FullActionA` = **56 effects** (was 46),
  faithful to dregg1 `apply.rs` (waves 1–4). Per-asset conservation VECTOR (`execFullA_ledger_per_asset`),
  capability non-amplification, the tree-lift (`FullForest`), the gated auth mirror (`FullForestAuth`).
  De-shadows VERIFIED real: `exerciseA` recurses inner effects; slot-caveats wired into `setFieldA`→
  `stateStepGuarded` (Immutable/MonotonicSequence/WriteOnce reject); seal/unseal actually move caps;
  lifecycle/factory real.
- **The coinductive crowns** — `Exec/Cell*.lean`: `livingCellA_carries` (ANY invariant carried forever
  vs any adversary) + conservation, confinement (cap-safety, the l4v lift), no-double-spend,
  commitment-persistence. The parametric userspace-safety engine.
- **The Hatchery** — `Verify/{Tactics,Frames,Contract,Catalog,Regression}.lean` (H1–H4): `carry_forever`/
  `exec_frame` tactics, the `[Dregg2]` aesop frame rule-set, `CellContract` + the shape-catalog macros
  (`monotone_registry%`/`conservation%`/`confinement%`/`automaton_inv%`), reproducing 6 shipped crowns
  with defeq witnesses. Plus `Widget/*` (ProofWidget guarantee-cards + `#dregg_badge` trust tiers).
- **The time foundation** — `Time/{Causal,Frame,Deadline}.lean` (Phase 1, NEW): causal_after (lightcone,
  on the lace) vs frame_within (attested, explicit δ), the `Deadline` sum type forcing the distinction,
  the `commit_wait_bridge` keystone.
- **Substrate**: BFT/CordialMiners (real DAG consensus model), Stingray budget, the LTL `Proof/Temporal`,
  Durability/WAL, the CRDT/drift ladder, caveat-chains/3p-discharge/selective-disclosure/designated-
  verifier, the 3-layer CryptoKernel + 8 §8 `@[extern]` portals, the apps
  (NameService/Identity/Subscription + RightOfWay constellation), `Consistency.lean` (non-vacuity
  witness). Comment corpus de-cruft'd + honesty-audited (every §8/OPEN/teeth marker preserved).

**Honest framing:** this is a strong verified *abstract + intermediate spec* with a genuine coinductive
safety story. It is NOT yet l4v-quality *throughout* — see §2.

---

## 2. The l4v-quality gap (where seL4-grade is, where we are)

| Axis | seL4/l4v | dregg2 today | gap → track |
|---|---|---|---|
| Abstract invariants | ✅ machine-checked | ✅ genuine (conservation/confinement/temporal/carried) | **near-parity** |
| Spec → implementation refinement | ✅ forward-sim, both directions | a *projection* + FFI differential; **0 prod call-sites call the verified kernel** | **LARGE → Track H (THE SWAP)** |
| Down to binary / compiler | ✅ translation validation | none (Lean→Rust Path-A FFI, trusted) | **LARGE** (long arc) |
| Confidentiality / info-flow | ✅ noninterference proof | a k-anonymity/view *model* only | **LARGE → Track C** |
| Liveness | (seL4 claims little) | partial (Stingray/CordialMiners safety; gossip/GST/dissemination OPEN) | medium → **Track D** |
| Crypto / circuit | tiny TCB | 8 `@[extern]` assumed; circuit ~2–5% at constraint level | **LARGE → Track J** |

The single highest-leverage move toward l4v-quality is **Track H (THE SWAP)** — make the verified kernel
the thing prod actually calls. Until then the theory sits *beside* the running system, not *of* it.

---

## 3. Privacy parity with dregg1 (the sharpest surpass-dregg1 gap)

dregg1 Rust has a rich privacy stack; dregg2 Lean verifies the *ledger/authority discipline around*
privacy but **almost none of the cryptographic privacy primitives**. (Inversion: Lean HAS a
`DesignatedVerifier`; Rust LACKS deniability/chameleon entirely.)

| dregg1 has | Rust source | dregg2 Lean | → |
|---|---|---|---|
| Stealth / one-time addresses (X25519 DH) | `wasm/src/privacy.rs` | none | **Track E** |
| Sealed boxes (X25519 + ChaCha20-Poly1305) | `tokenizer/src/encrypt.rs` | none (only the `SealKernel` portal) | **Track E** |
| Encrypted turns + bloom conflict-set | `turn/src/encrypted.rs` | none | **Track E** |
| Searchable symmetric encryption | `intent/src/sse.rs` | none | **Track E** |
| Garbled circuits (Yao + Poseidon2) | `circuit/src/garbled.rs` | none (gallery's private Vickrey uses it!) | **Track E** |
| Blinded-leaf multi-show unlinkability | `bridge/src/present.rs` | only the `BlindedSet` holder-anon *predicate*, NOT the unlinkability argument | **Track E** |
| ZK/STARK/FRI soundness | `circuit/` | all §8-assumed | **Track G** (honest partial) |

We verify that the bookkeeping around private ops is sound; we do not yet verify the crypto is sound.

---

## 4. THE ROADMAP — all tracks (Opus only for proof/feature work)

Tracks are independent unless noted. **Track A is the active frontier**; the rest are the standing
program (the "flesh out dregg2, become a metatheory of web3, surpass dregg1, reach l4v-quality" goal).
Build features **in tandem with the demonstrator apps** that prove them right (ember: a wrong model is
worse than none — the apps are the ground truth).

### Track A — Intent-as-co-receipt metatheory (ACTIVE, 4-phase, ember-approved)
The metatheory-of-web3 spine. Phase 1 DONE.
- **Phase 1 ✅** — the time foundation (`Time/{Causal,Frame,Deadline}`, commit `6ed3df7a`).
- **Phase 2 ← NEXT** — the `Intent` core. **Layered option (c)**: abstract `Intent` over a
  `[ResourceTheory R]` + concrete `KernelIntent`. `Match` = mathlib's coend (it ALREADY exists). The
  receipt⊣intent adjunction proved incrementally. **Full spec: `PHASE-2-INTENT-SPEC.md`.**
- **Phase 3** — conservation-as-`AddMonoidHom`-invariant (mathlib reuse) + the userspace-escrow
  cell-program, proving **userspace-escrow ≥ kernel-escrow** (ember's hard bar; safety becomes
  corollaries of global kernel invariants — *stronger*, not weaker). This is the gate to retract kernel
  escrow.
- **Phase 4** — the **gallery sealed-bid auction** on all of it: prove (a) userspace-escrow ≥ kernel,
  (b) causal reveal-ordering excludes frontrunning, (c) conservation across settle. Benchmarks vs
  Penumbra (a proof, not a DKG trust assumption).
- DeFi beyond the auction (Track A continued): swap/limit-order, AMM-as-standing-offer, lending — each
  = intent + escrow + time + match + conservation. MEV honesty boundary: causal ordering excludes
  reorder-MEV-of-causally-ordered-events + conservation excludes mint-MEV, but **cannot** give a fair
  total order over genuinely-concurrent intents (Condorcet-bounded — `INTENT-REFS-web3.md`). "All MEV
  abolished" is FALSE; use a γ-batch tie-break.

### Track B — Powerful temporality: CTL / μ-calculus
Beyond the existing LTL (`Proof/Temporal`). Target `Proof/CTL.lean` (EX/AX/EU/AU/EF/AF/EG/AG as named
`OrderHom.lfp`/`gfp` over `Set Config`) + `Proof/MuCalculus.lean` (μ/ν, encode CTL as μ-formulae).
**`Mathlib.Order.FixedPoints` is already a dep** (via Paco); Lean 4.30 has `coinductive`. Port-thin
CSLib's LTS + Hennessy–Milner for the modal side (`EXTERNAL-LEAN-REFERENCES.md`). Non-vacuity: a small
branching system where `EX p ≠ AX p` and `EF p ≠ AF p`. Composes with Track A (auction *phases* are
temporal).

### Track C — Confidentiality: noninterference / information-flow (greenfield)
No Lean IFC exists. Target `Proof/Noninterference.lean`: a `SecurityLattice`, `lowEq` on
`RecChainedState` (reuse `Privacy.project`/a chosen low field-set), and a Volpano-Smith/seL4 unwinding
theorem for a slice of public effects (`lowEq s t → lowEq (execFullA s fa) (execFullA t fa)`), with a
LEAK counterexample as teeth (an effect that writes a low field from a high one breaks `lowEq`). This is
the l4v confidentiality axis (§2). Sealed bids (Track A/E) are the demonstrator.

### Track D — Liveness: fairness / GST
Target `Proof/Fairness.lean` (`Fair : SchedA → Prop` as weak/strong fairness □◇; `Fair ∧ progress-enabled
⟹ Eventually(goal)` — the existing ◇ is trivial-now-form) + `Proof/GST.lean` (partial-synchrony GST
feeding `Proof/BFTLiveness.Pacemaker`/`Synchronizer`). Port-thin Lentil's □/◇ vocab (Apache,
`EXTERNAL-LEAN-REFERENCES.md`). Closes the named consensus OPENs (gossip dissemination/GST). Loser-refund
liveness in the auction is the demonstrator.

### Track E — The privacy-crypto stack (surpass-dregg1 privacy parity)
New `Crypto/` modules, each: a structure + ONE security property *carrier-conditional on a §8 portal*
(NEVER `fun _ _ => True` — that exact vacuity was killed before). Port-thin VCVio's game-logic
(`INTENT-REFS`/`EXTERNAL-LEAN-REFERENCES`):
- `Crypto/Stealth.lean` (one-time addrs, unlinkability) ← `wasm/src/privacy.rs`
- `Crypto/SealedBox.lean` (X25519+AEAD, open∘seal round-trip, fail-closed teeth) ← `tokenizer/src/encrypt.rs`
- `Crypto/EncryptedTurn.lean` (decrypt-correctness + bloom metadata-privacy) ← `turn/src/encrypted.rs`
- `Crypto/SSE.lean` (search-token soundness + keyword privacy + honest leakage-profile note) ← `intent/src/sse.rs`
- `Crypto/Garbled.lean` (eval=f(x) + simulatable view = garbled-circuit privacy) ← `circuit/src/garbled.rs`
- `Crypto/BlindedPresentation.lean` (the SHARPEST: multi-show unlinkability with fresh-vs-reused-blinding
  teeth — the argument `BlindedSet` lacks) ← `bridge/src/present.rs`
These ARE the gallery private-Vickrey's primitives — build in tandem with the auction.

### Track F — Caveat / predicate completion (the only real effect/caveat gaps)
All 56 effects modeled. Remaining: `Authority/CoordinatedCaveat.lean` (promote `CrossCaveat`'s
fail-closed `.coordinated` to a real coordinated discharge + no-TOCTOU) and `Authority/BiscuitGraph.lean`
(the biscuit public-key delegation-block DAG; prove granted≤held along any path, mirroring
`CDT.path_attenuates`). Teeth: over-broad discharge / amplifying block rejected.

### Track G — ZK-soundness probe (honest partial; ember: full ZK won't land)
`Crypto/ZkSoundness.lean`: a Sigma-protocol (Schnorr) algebraic extractor (special-soundness, NO
rewinding axiom — VCVio-style) + HVZK simulator. Explicit: PROVED (sigma algebra) vs ASSUMED (§8 group
carrier) vs OPEN (full STARK/FRI knowledge-soundness — mark clearly).

### Track H — THE SWAP (the highest-leverage l4v move) ⚑
Make the verified kernel the thing prod actually calls (today **0 prod call-sites** do). The Rust half:
the Turn→§WIDE-wire marshaller + wire→TurnResult unmarshaller + prod call-site rewire, behind a
full-surface differential CI ratchet (kernel-vs-NEW-Rust, NEVER vs the buggy dregg1 oracle — matching a
buggy oracle launders the bug). See `project-the-swap-roadmap.md` + `project-swap-framing.md`. The
@[export] gate had a cluster of soundness holes the verify-agents rationalized — DO NOT cut over until
re-audited with the lens "can an adversarial caller make this gate-leg true for free?".

### Track I — The apps (the ground truth for A/C/E)
gallery sealed-bid + private-Vickrey auction (the Track A/E proving ground), private orderbook,
compute-exchange, privacy-voting, bounty-board, and harden the 4 starbridge-apps
(nameservice/identity/subscription/governed-namespace) on the real factory-slot-caveat enforcement. Each
proves its headline via the Hatchery.

### Track J — Circuits (DEFERRED until dregg2 "finished", ember)
Lean emits a 6-col/4-constraint TOY; the live prover is a 126-col EffectVmAir (~2–5% modeled at
constraint level). Proof-carrying-forest strategy (ship the tree of per-step proofs + linking witness;
aggregation = deferred PERF, the composition glue is already proved). Do NOT frame Plonky3 as turnkey
recursion. Pick up after the metatheory is fleshed out.

### Track K — Misc / foundational
`Containers/CSpace` (tier-3 sandbox → migrate the executor, #140); `Disputation/Lawvere` (the corrected
reflector → dual-H¹ theorem, #141 — see `project-adjunction-thesis-verdict.md`); dregg4 forward vision
(`project-dregg4-vision.md` — the guarded comodel/lens, the disclosure dial-cube, the single-machine
principle, hypersystem/simplicial generalization).

---

## 5. Discipline (NON-NEGOTIABLE — ember has corrected on each)

- **OPUS ONLY for feature/proof development.** NEVER sonnet for features. Sonnet only for mechanical
  comment cleanup or simple Explore reference-enumeration.
- **Read the ground truth YOURSELF** — don't delegate *understanding*. Read-only agents are for fan-out
  search / reference-gathering, not the core "what does this code mean" judgment. (ember spelled this
  out: gather points/references/comparisons with Sonnet Explore, THEN issue the big Opus workflows.)
- **Features go IN TANDEM with demonstrator apps.** A wrong model is worse than none — we'd be committed
  to it. Bring every model-shape call to ember.
- **NEVER `git checkout`/reset/stash to discard WIP.** Broken tree from a workflow → WIP-commit
  (`"WIP [BROKEN BUILD]: ..."`) to preserve, then fix-forward. Never revert.
- **Gate the FULL build yourself in the main loop before committing** any agent output. Green standalone
  module ≠ green library. `#assert_axioms`-clean ≠ faithful/non-vacuous — READ the load-bearing bodies
  (verify-don't-trust). A green build hides nothing about FIDELITY — audit dregg1-vs-Lean per effect.
- **Build discipline:** `(cd …/metatheory && ~/.elan/bin/lake build <M>) > /tmp/x.log 2>&1; echo
  "EXIT=$?"` — read the log, not just `$?` (a trailing `grep -c` flips the shell's exit on a clean build).
- **Feature workflows** = Opus draft + Opus verify-and-repair (adversarial de-vacuification — it
  repeatedly catches its own vacuities, e.g. the time-foundation `twoδ=2δ` carrier). Each builds its
  file green; the main loop does the final full-build gate + single commit. Sequential when the shared
  `FullActionA` dispatch is touched; parallel only for import-disjoint files. Don't poll running agents.
- 1Password may reject signing when ember is away → `--no-gpg-sign` is fine (flags away-work).
- Funding note: ember is working toward a second concurrent sub (+$200/mo) — until then, one agent at a
  time on the long arc.

---

## 6. Open fidelity gaps (from `EFFECT-FIDELITY-LEDGER.md`) — carry these

NOT shadows (real state machines), but named: `exerciseA` gate is connectivity-only (facet-mask/expiry/
revocation not executable — ember chose "close with the clock/intent work"); `fulfill/slashObligationA`
thread `actor` into no gate (likely subsumed by userspace-escrow + Deadline now); committed-escrow
`release/refund` are SILENT shadows (aliased to plain escrow) — **dying via escrow→userspace, not
patched**. Two kinds of time kept apart: causal (the lace) vs frame (attested, δ) — DONE in Track A
Phase 1.

---

## 7. First move for the next Claude

Build green (3500 jobs). Read §0. The frontier is **Track A Phase 2** (`PHASE-2-INTENT-SPEC.md`) — but
any of Tracks B–K is real, independent work toward the §1 vision. Opus; gate the full build yourself;
bring model-shape calls to ember. Hold the frame: *what is the witness, and whose construction is it?*
