# Rust↔Lean Divergence Ledger

Generated by `turn/tests/rust_lean_divergence_finder.rs` — runs the REAL Rust executor (`apply.rs`) and the VERIFIED Lean executor (`dregg_exec_full_forest_auth` FFI) side-by-side over a corpus of turns and records every divergence.

- `DREGG_LEAN_SHADOW`/`lean-shadow` active: **true** (false ⇒ Lean not linked; the table records the eligibility map only — every effect shows GAP).
- Corpus turns: **15** | comparable (Lean modelled): **11** | **DIVERGENCES: 3**

Verdict legend: **AGREE** = Lean modelled the effect and matched apply.rs's commit bit. **DIVERGE** = modelled and DISAGREED (a real drift/bug). **GAP** = no Lean wire model yet (turn ineligible for shadow; the remaining swap surface, not a bug).

## Per-turn

| case | effects | rust | lean | verdict |
|------|---------|------|------|---------|
| SetField | SetField | Committed | lean=Some(true) | AGREE |
| Transfer | Transfer | Committed | lean=Some(true) | AGREE |
| Transfer/overspend | Transfer | Rejected(InsufficientBalance { cell: CellId(8b43f1da), required: 9999, available: 10 } @[0]) | lean=Some(true) | DIVERGE (rust=false, lean=Some(true)) |
| Transfer/overspend2 | Transfer | Rejected(InsufficientBalance { cell: CellId(8b43f1da), required: 1000, available: 100 } @[0]) | lean=Some(true) | DIVERGE (rust=false, lean=Some(true)) |
| SetPermissions | SetPermissions | Committed | lean=Some(true) | AGREE |
| SetVerificationKey | SetVerificationKey | Committed | lean=Some(true) | AGREE |
| EmitEvent | EmitEvent | Committed | lean=Some(true) | AGREE |
| MakeSovereign | MakeSovereign | Committed | lean=Some(true) | AGREE |
| NoteCreate | NoteCreate | Committed | lean=Some(true) | AGREE |
| NoteSpend | NoteSpend | Rejected(InvalidEffect { reason: "NoteSpend missing spending proof" } @[0]) | lean=Some(true) | DIVERGE (rust=false, lean=Some(true)) |
| SetField+Transfer | SetField+Transfer | Committed | lean=Some(true) | AGREE |
| IncrementNonce | IncrementNonce | Committed | lean=None | GAP (no Lean model) |
| Burn | Burn | Committed | lean=None | GAP (no Lean model) |
| CellSeal | CellSeal | Committed | lean=None | GAP (no Lean model) |
| GrantCapability | QueueAllocate | Committed | lean=None | GAP (no Lean model) |

## Per-effect map

| effect | modelled | agree | diverge | gap | error | note |
|--------|----------|-------|---------|-----|-------|------|
| Burn | NO | 0 | 0 | 1 | 0 |  |
| CellSeal | NO | 0 | 0 | 1 | 0 |  |
| EmitEvent | yes | 1 | 0 | 0 | 0 |  |
| IncrementNonce | NO | 0 | 0 | 1 | 0 |  |
| MakeSovereign | yes | 1 | 0 | 0 | 0 |  |
| NoteCreate | yes | 1 | 0 | 0 | 0 |  |
| NoteSpend | yes | 0 | 1 | 0 | 0 | rust_committed=false lean_committed=Some(true) |
| QueueAllocate | NO | 0 | 0 | 1 | 0 |  |
| SetField | yes | 2 | 0 | 0 | 0 |  |
| SetPermissions | yes | 1 | 0 | 0 | 0 |  |
| SetVerificationKey | yes | 1 | 0 | 0 | 0 |  |
| Transfer | yes | 2 | 2 | 0 | 0 | rust_committed=false lean_committed=Some(true) |

## Findings (characterised drift)

The agreement comparison is COMMIT-BIT-ONLY (the shadow compares whether both executors commit/roll back, not the full post-state). With that lens:

1. **Modelled & agreeing**: `SetField`, `SetPermissions`, `SetVerificationKey`, `EmitEvent`, `MakeSovereign`, `NoteCreate`, and authorised `Transfer` (e.g. 30/100) — apply.rs and the verified Lean executor agree on the commit decision. The handler-audit suspects (`MakeSovereign`/`receiptArchive`/`queueAllocate`): `MakeSovereign` is now MODELLED and AGREES; `ReceiptArchive`/`QueueAllocate` remain GAP (no Lean wire projection yet — not a divergence).

2. **Known drift (allowlisted)** — `Transfer` overspend & `NoteSpend`: the `lean_shadow` marshaller emits the turn under `Unchecked` auth with an EMPTY caps table and DROPS the `note_tree_root`. Consequence: the Lean per-asset executor commits these as a **NO-OP** (loglen 0, no state change — it neither applies nor rejects), while apply.rs either REJECTS on a precondition the projection cannot see (overspend availability; NoteSpend null `note_tree_root`) or APPLIES the move. These are **MARSHALLER-faithfulness gaps** (the shadow wire under-specifies auth/caps and drops note fields), not verified-kernel logic bugs. To close them: marshal the real credential + caps so the Lean per-asset gate is genuinely exercised, and carry the note_tree_root in the `notespend` wire arm. Until then they are recorded, not failed.

3. **GAP (no Lean model yet)** — `IncrementNonce`, `Burn`, `CellSeal`, `QueueAllocate`, and the ~35 other effects: not projected to the Lean wire, so the turn is INELIGIBLE for shadow. This is the remaining SWAP surface — what still needs a Lean model + wire arm before the full cutover. The 9 currently-modelled effects are: SetField, Transfer, SetPermissions, SetVerificationKey, EmitEvent, MakeSovereign, RevokeDelegation, NoteSpend, NoteCreate.

## How to run

```
DREGG_LEAN_SHADOW=1 cargo test -p dregg-turn --features lean-shadow --test rust_lean_divergence_finder -- --nocapture
```

Requires `dregg-lean-ffi/libdregg_lean.a` (the compiled Lean closure) present + the project Lean toolchain (build.rs resolves the sysroot via `lake env`). Wire into CI / devnet by running the node with `DREGG_LEAN_SHADOW=1` and the `lean-shadow` feature — `lean_shadow::maybe_shadow_turn` then logs live divergences (target `dregg::lean_shadow::divergence`) for every executed turn.
