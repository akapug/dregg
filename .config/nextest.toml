# ─────────────────────────────────────────────────────────────────────────────
# Heavy / slow test segregation
# ─────────────────────────────────────────────────────────────────────────────
# The default gauntlet must stay FAST. A handful of recursion / IVC / fold /
# proptest suites legitimately run for *minutes* in debug — they dominate the
# wall-clock of `cargo nextest run`. They are excluded from the default/ci
# profiles' `default-filter` and run on demand via `--profile heavy`
# (preferably `--profile heavy --release`, see [profile.heavy] below).
#
# These are NOT disabled — they are *segregated*. The split is by NEXTEST
# CONFIG ONLY (no source edits): every heavy test lives in a crate that is
# under active rewrite, so its `.rs` must not be touched here.
#
# ⚠ THE GAUNTLET WAS DEAD, AND THIS FILE IS WHY (fixed 2026-07-15).
# `effect_vm_descriptor_cutover_harness` was listed in the three filtersets below.
# That test binary was DELETED in `384d0cd5a` and has no survivor. nextest treats
# an unresolvable `binary()` name as a hard config-PARSE error — not a warning, and
# not scoped to the profile you selected: it validates EVERY profile in this file
# on every invocation. So `scripts/test-gauntlet.sh` exited nonzero in EVERY mode
# (default / ci / full / heavy) from that commit onward, having run zero tests.
# The gauntlet did not "not pass --ignored"; it did not run at all.
#
# The name outlived its referent (pattern P2) — in the test infrastructure itself,
# where it silently disabled the entire suite rather than merely misleading a
# reader. If you delete or rename a test binary, grep THIS FILE for its name.
#
# ⚠⚠ AND THE ABOVE PARAGRAPH DESCRIBES THE LOUD HALF ONLY. MEASURED 2026-07-27,
# against cargo-nextest 0.9.136/0.9.137/0.9.140, with a two-test scratch crate:
#
#   binary(no_such_name)   → HARD ERROR, exit 96, "operator didn't match any binary names",
#                            raised while validating EVERY profile, on EVERY invocation.
#   package(no_such_name)  → HARD ERROR, exit 96, "operator didn't match any packages".
#   test(~no_such_name)    → **SILENT. exit 0. Not a warning. Nothing is printed at all.**
#
# So nextest self-defends exactly two of the three operators this file uses, and the
# 2026-07-15 incident was the FORTUNATE case: it screamed. A `test(~…)` name that outlives
# its referent produces no diagnostic ever, and what it does next depends on its POLARITY:
#
#   * in a `not (…)` EXCLUSION (default / ci): the clause excludes nothing. Harmless to
#     correctness, but the file now states a quarantine that is not in force.
#   * in a POSITIVE selector ([profile.heavy], [profile.gpu]): the test is silently NOT RUN
#     by the profile whose whole purpose is to run it. A renamed heavy test simultaneously
#     drops OUT of `heavy` and — because the default/ci exclusion keys on the same dead
#     name — reappears INSIDE the fast default gauntlet it was segregated out of. A renamed
#     GPU tooth drops out of `gpu` and into `armed`, i.e. onto GPU-less boxes, where it is
#     the guaranteed red the GPU exclusion below exists to prevent.
#
# THIS IS NOT HYPOTHETICAL AND IT HAD ALREADY HAPPENED HERE. The same 2026-07-27 sweep
# resolved all 41 `test(~…)` names in this file against the tree: **21 of them matched no
# `fn` anywhere in the workspace.** The entire kimchi quarantine — 19 `test_kimchi_*` names
# plus `test_p0_2_derivation_honest_still_verifies` and
# `test_section5_assertion_rejects_mismatch` — named tests deleted in `d260028f1` ("the
# great deletion: ~29K lines of legacy backends gone") and `136e51446`. There is no kimchi
# backend in this tree; `rg kimchi` finds one path, a `sketches/` probe manifest. The
# comment below them called them "currently failing / timeout kimchi-backend diagnostics",
# present tense, about tests that had not existed for weeks. They are removed now, and
# removing them is a no-op on execution BY CONSTRUCTION: an exclusion of a test that does
# not exist excludes nothing.
#
# `scripts/check-nextest-names.py` is the check that would have caught both halves on the
# day, and it is wired into `scripts/local-gates.sh`. It resolves `binary()`/`package()`
# exactly (via `cargo metadata`, no compile) and `test()` against the tree's own `fn`
# declarations. Run it before you delete or rename ANY test.
#
# The heavy set (name · crate · ~debug duration in the reference gauntlet):
#   binary descriptor_leaf_recursion              circuit  ~28.5s  (1 test)
#   binary rotation_batchstark_leaf_smoke         circuit  ~342.4s (2 tests, both >60s each)
#   binary proptest_invariants                    turn     ~288.9s (5 tests, all >60s each)
#   test   k_fold_turn_chain_proves_and_verifies            circuit (lib, >60s)
#   test   two_step_inductive_core_proves_and_verifies      circuit (lib, >60s)
#   test   foreign_circuit_root_is_refused_by_vk_pin        circuit (lib, >60s; shares the fold setup)
#   test   shielded_whole_note_swap_substrate_hiding_proof_and_hostile_publics
#                                                           circuit-prove (15k-wide HidingFRI proof)
#   test   authenticated_encrypted_orders_gate_real_game_asset_settlement
#                                                            dreggnet-market (>1080s release)
#          The same name prefix also routes fhegg-fhe's isolated verified-share
#          prove/verify crypto tooth, so engine performance can be measured
#          without compiling the market/persistence integration graph.
#   test   private_bazaar_winner_spends_the_proof_assigned_mender_through_signed_spine_once
#                                                            dreggnet-catalog (~76s release)
#   test   production_bfv_book_a_and_valid_hidingfri_book_b_cannot_cross_pair
#                                                            fhegg-fhe (production root-link tooth)
#   binary private_attested_clearing                          dreggnet-market (HidingFRI prove)
#   binary private_book_bfv_zk                               fhegg-fhe (n=4096 Bulletproof R1CS)
#   binary energy_dispatch                                   fhegg-fhe (three exact TFHE candidate evaluations)
#   binary private_bfv_attested_clearing                     dreggnet-market (composite apex)
#   binary private_clearing_apex_e2e                         dreggnet-market (proof + restart + asset)
#   binary poa_dark_bazaar_protocol                          dreggnet-market (PoA-bound private clearing)
#   binary private_clearing_crown_consequence                dreggnet-market (proof + game consequence)
#   binary private_clearing_guild_allocation                 dreggnet-market (proof + guild allocation)
#   binary private_bazaar_playable_journey                   dreggnet-market (proof + shared game surface)
#
# ⚑ ADDED 2026-07-27 — `dregg-multiway-tug`'s six `#[ignore]`d real-cell folds. They are NOT in the
# heavy SET (they use the other mechanism, `#[ignore]`), but they DID need a real BUDGET, and this
# file's own TIMEOUT DOCTRINE is that those are two separate questions. MEASURED: run with
# `--run-ignored ignored-only`, `private_match_folds_and_lightclient_accepts` and
# `match_win_output_is_attested` were both TERMINATED at exactly 180.1s — `[profile.default]`'s
# 45s x 4 kill, inherited by a `--run-ignored` invocation nobody had ever pointed at this crate. So
# six deliberate multi-minute recursion folds were structurally UN-RUNNABLE under nextest and could
# be driven only by bare `cargo test`. A budget that truncates a real 5-minute fold is exactly the
# failure this file calls worse than having no budget at all.
#   test private_match_folds_and_lightclient_accepts                       multiway-tug (2 REAL play turns)
#   test match_win_output_is_attested                                      multiway-tug (play + win, one chain)
#   test membership_play_folds_over_the_real_cell_and_lightclient_accepts  multiway-tug
#   test win_folds_over_the_real_cell_and_lightclient_accepts              multiway-tug
#   test app_root_weld_refuses_disagreeing_winner_through_verify_history   multiway-tug (3 folds)
#   test the_leg_octet_exposes_the_committed_field_values                  multiway-tug
#
# ⚑ ADDED 2026-07-30 — four `dregg-node` tests that are legitimately slow and were BUDGETED, not
# segregated (measured durations + reasoning at their `[[profile.default.overrides]]` row below):
#   test   quorum_decision_matches_verified_lean_gate         node (lib; was TIMING OUT at 180s)
#   binary retained_history_ivc_seam                          node (172.9s — 7s under the old kill)
#   test   retained_finalized_turn_round_trips_and_readmits   node (lib, 126.4s)
#   binary n3_plateau_probe                                   node (113.5s)
#
# ⚑ ADDED 2026-07-30 (a FIFTH `dregg-node` row, same day, same reasoning) —
#   binary payoff_client_turn                                 node (81.0s green; 180.1s KILLED before)
# It spawns FOUR real `dregg-node` processes, meshes them over loopback QUIC, and drives an
# external-client-signed turn through consensus. Its floor is structural, not incidental: a 45s
# port wait, an 8s settle, a 90s faucet-finality wait and a 90s client-turn-finality wait, so its
# legitimate worst case is ~4 minutes. It measured 180.051s and was reported as a FAILURE — it was
# KILLED by `[profile.default]`'s 45s x 4, mid-wait, and the kill was read as a consensus defect for
# long enough to matter. It is now 81.007s green on hbox (measured 2026-07-30 after the three
# dissemination fixes in `blocklace_sync.rs`/`finalization_votes.rs`), which is 45% of the old kill
# and would have been a coin-flip on a busy box. BUDGETED, not segregated: it is the only test in
# the tree that proves an external client's own signed turn reaches the DAG and commits cross-node,
# and it now carries the refusal pole too (forged ed25519 / stripped ML-DSA / agent substitution /
# substituted ML-DSA signer / replay, each refused BY VARIANT). Moving it to `heavy` would take the
# whole external-ingress gate out of the everyday gauntlet.
# The three circuit-LIB tests are excluded by name (their binary, dregg-circuit's
# lib, holds ~960 other fast tests we keep). The integration suites above
# are excluded whole-binary. Binary/test names are unique workspace-wide.
#
# Already-`#[ignore]`'d minute-scale tests (not listed here because nextest skips
# them by default anyway): proof_economics.rs::t3_ivc_root_k2 / t3_ivc_root_k3.

# Reusable heavy-filterset fragment is inlined into each profile's
# `default-filter` below (nextest has no filterset aliases in 0.9.x config).

# ── Standing preflight quarantine (pre-existing) ─────────────────────────────
# The preflight meta-suites, excluded from default/ci.
#
# ⚠ THIS SAID "All 7 names verified" AND THE TREE HAS 36 `preflight_*` TESTS. The other 29 are not an
# oversight — most are fast and belong in the everyday gauntlet. But a 2026-07-31 whole-workspace
# measurement found FIVE of them dying on the 180 s kill (`backends`, `composition`, `lean_marshal`,
# `node`, `sovereign`), i.e. quarantined in intent and not in fact. Added exactly those five — the
# measured set, not a blanket sweep, because quarantining a test that would pass is how coverage
# leaves silently.
# The 12 names now verified to
# resolve 2026-07-27. (The kimchi half of this quarantine was 21 names for tests
# that no longer exist — see the ⚠⚠ block at the top of this file. Removed.)

# ─────────────────────────────────────────────────────────────────────────────
# THE TIMEOUT DOCTRINE — read this before changing any `slow-timeout`
# ─────────────────────────────────────────────────────────────────────────────
# `slow-timeout = { period = P, terminate-after = N }` is TWO knobs and they are
# not the same decision:
#
#   period P        — when to PRINT a `SLOW` line. Costs nothing, kills nothing.
#                     Set it TIGHT. It is pure information.
#   terminate-after N — kill the test at P×N. This is the gate, and it is the
#                     only number that can destroy a real result.
#
# WHY EVERY PROFILE NOW HAS A KILL. Three classes of test in this workspace hang
# FOREVER, and a hang is uniquely destructive to a test run: it does not fail, it
# denies a verdict to everything queued behind it. The first complete
# `cargo test --workspace` (2026-07-26) needed an external watchdog for exactly
# this — /tmp/ws-watchdog.log records it killing
# `tlsn_live_roundtrip` by hand (then in `dregg-zkoracle-prove`, now in
# `dregg-zkoracle-live`). Under process-per-test
# with a kill budget, that same hang is a NAMED FAILING TEST and the other ~16 000
# still report. `[profile.heavy]`, `[profile.armed]` and `[profile.gpu]` had NO
# `terminate-after` at all — a hang in any of them ran until a human noticed, which
# for a nightly means "until someone wonders why there has been no report".
#
# ⚠ A TIMEOUT IS NOT A FIX, AND MUST NOT BE READ AS ONE. The three hang classes have
# real, diagnosed root causes and a separate lane owns them:
#   dregg-zkoracle-live   a genuine DEADLOCK (98 threads parked in futex_do_wait) — the MPC-TLS
#                         2PC suites, which lived in `dregg-zkoracle-prove` until the
#                         2026-07-27 crate split. ⚠ BOTH packages stay in the filter below:
#                         `dregg-zkoracle-prove` keeps the fixture-path suites, and a bound
#                         that silently stopped covering the moved binaries would be a
#                         deleted gate wearing a rename.
#   servo-render          waits on a display a headless box does not have
#   starbridge-tussle     99.9% CPU spinning on a loop that discards a Result
# The overrides below bound each one's blast radius. They do not repair any of them,
# and when the owning lane lands the fixes these overrides should be DELETED, not
# retuned. What they buy is that one bad loop can no longer take the whole signal
# down with it — and, because nextest reports a killed test as `TIMEOUT` and counts
# it toward the failure total (verified: exit 100, "1 timed out"), the bound is
# LOUD. A timeout here can never read as a pass and never as a skip.
#
# ⚠ AND THE OPPOSITE ERROR IS WORSE. A kill budget that truncates a real 5-minute
# recursion fold is a worse gate than no budget at all: it manufactures a red that
# says nothing and trains readers to ignore the runner. So NO number below is a
# guess — each is the duration this file already records for that suite, times a
# margin, and the heavy set is granted its budget BY NAME rather than by loosening
# the global. The recorded maximum in this workspace is >1080s (dreggnet-market's
# `authenticated_encrypted_orders_gate_real_game_asset_settlement`, and that is its
# RELEASE figure — `--profile full` runs debug, where it is far slower). The heavy
# override is 3600s: three-plus times the slowest thing ever measured here, which
# is "hung" by any reading, and still a bound.
#
# ⚠ `[[profile.default.overrides]]` IS INHERITED BY EVERY CUSTOM PROFILE (verified
# 2026-07-27: a rule written here fired under `--profile child`). That is why the
# heavy-set budgets live here once instead of being copied into `full` and `heavy`,
# and it is why adding a rule here is a WORKSPACE-WIDE act, not a default-lane one.

# The legitimately-slow set. Named individually, so that granting them a real budget
# does not also grant one to a deadlock. Durations in the table at the top of this file.
#
# ⚠ THE SECOND HALF OF THIS LIST WAS NOT IN THE HEAVY SET, AND THAT IS THE POINT.
# The heavy set is the suites someone SEGREGATED for being slow. It is not the same
# thing as the suites that ARE slow, and this file's `[profile.default]` asserted
# "with the heavies gone, default tests are well under a minute" — which the
# per-binary durations of the 2026-07-26 reference run refute outright:
#
#   multi_round_fold                    1879.2s / 17 tests  (~111s each)
#   ext_degree_cost_measure             1032.5s /  2 tests  (~516s each)
#   catalog_flow_harness                 859.3s /  4 tests  (~215s each)
#   e2_fold_arity_recompose_probe        450.9s /  2 tests  (~225s each)
#   private_raid_player_journey          369.0s
#   tiny_automata_prover_shape_measure   279.0s /  5 tests
#   note_spend_binding_node_tooth        193.2s /  1 test
#   hosted_party                         182.6s /  2 tests
#
# Not one of these is in any profile's heavy clause, so all of them run in `default`
# — against a 45s×4 = 180s kill. `ext_degree_cost_measure` alone averages 516s per
# test. Adding a `terminate-after` without checking this would have manufactured a
# fresh red in the DEFAULT gauntlet for at least four suites doing exactly what they
# are supposed to do, which is the precise failure this file's TIMEOUT DOCTRINE calls
# worse than having no budget at all. They are granted the budget here rather than
# segregated into `heavy`, because how long a suite may legitimately take is a
# different question from which profile ought to run it, and only the first is a
# timeout's business. Durations are from the reference run; re-measure before trimming.
# The September 17 name check also found one stale budget-only selector,
# membership_play_folds_and_lightclient_accepts: no function in the current
# tree has that name. Its removal changes no selected test or kill budget for
# an existing test; the other real-cell fold selectors remain below.

# Measured on hbox 2026-09-18 after lazy route registration: SDK candidate
# tests take0.026–0.047s and are back in the default set. The exact js-agent
# bridge still takes191.097s: startup is<10ms, but the first actual verified
# admission initializes the archive. Keep only that test heavy until the
# real gate's initialization path is repaired; do not bypass its Lean call.
[[profile.default.overrides]]
filter = 'test(~run_js_over_the_world_bridge_lands_on_the_served_world)'
slow-timeout = { period = "60s", terminate-after = 8 }

# The isolated lazy-registration regression deliberately makes its first real
# full-byte Lean ML-DSA call. Measured 188.347s on hbox (2026-09-18); it belongs
# to the heavy set even after SDK construction becomes cheap.
[[profile.default.overrides]]
filter = 'test(~pq_route_discovery_is_lazy_but_first_real_call_initializes)'
slow-timeout = { period = "60s", terminate-after = 8 }

# Fresh-process Lean lifecycle probes deliberately run the actual full initializer.
# They retain its measured ~191s cost while testing narrow/full ordering and native threads.
[[profile.default.overrides]]
filter = 'test(~lean_init_lifecycle_heavy)'
slow-timeout = { period = "60s", terminate-after = 8 }

[[profile.default.overrides]]
filter = 'test(~quorum_decision_matches_verified_lean_gate) or test(~exact_tuple_installs_and_crash_reopens_the_same_head) or binary(retained_history_ivc_seam) or test(~retained_finalized_turn_round_trips_and_readmits) or binary(n3_plateau_probe) or binary(payoff_client_turn) or binary(turn_anchor_binds_a_proof_to_the_committee) or binary(the_served_pair_binds_the_proofs_commitments) or binary(pasta_derive_prove) or binary(cap_open_write_prove_through) or binary(multi_round_fold) or binary(ext_degree_cost_measure) or binary(catalog_flow_harness) or binary(e2_fold_arity_recompose_probe) or binary(private_raid_player_journey) or binary(tiny_automata_prover_shape_measure) or binary(note_spend_binding_node_tooth) or binary(sovereign_binding_deployed_tooth) or binary(hosted_party) or binary(rotation_batchstark_leaf_smoke) or binary(proptest_invariants) or binary(descriptor_leaf_recursion) or binary(private_attested_clearing) or binary(private_book_bfv_zk) or binary(private_book_distributed_bfv_exact) or binary(private_bfv_attested_clearing) or binary(private_clearing_apex_e2e) or binary(poa_dark_bazaar_protocol) or binary(private_clearing_crown_consequence) or binary(private_clearing_guild_allocation) or binary(private_bazaar_playable_journey) or binary(energy_dispatch) or test(~k_fold_turn_chain_proves_and_verifies) or test(~two_step_inductive_core_proves_and_verifies) or test(~shielded_whole_note_swap_substrate_hiding_proof_and_hostile_publics) or test(~foreign_circuit_root_is_refused_by_vk_pin) or test(~authenticated_encrypted_orders_gate_real_game_asset_settlement) or test(~production_bfv_book_a_and_valid_hidingfri_book_b_cannot_cross_pair) or test(~private_bazaar_winner_spends_the_proof_assigned_mender_through_signed_spine_once) or test(~private_match_folds_and_lightclient_accepts) or test(~match_win_output_is_attested) or test(~win_folds_over_the_real_cell_and_lightclient_accepts) or test(~app_root_weld_refuses_disagreeing_winner_through_verify_history) or test(~the_leg_octet_exposes_the_committed_field_values) or test(~both_poles_through_the_deployed_prover)'
# 120s × 30 = 3600s. Warn at two minutes (all of these exceed it — the SLOW line is
# how you notice one has regressed), kill at an hour (none has ever come close).
#
# `pasta_derive_prove` joined this row on 2026-07-29, BUDGETED rather than segregated into
# `heavy`, on the same reasoning: it is a security tooth — four tampers (substituted generator,
# wrong-block scalar, absorbing state, challenge-inconsistent derivation) plus a both-polarity
# control and a canonicity probe, every one of them a real `prove_vm_descriptors2_batch` roundtrip
# against a 2,131-column / 1,024-row Lean-emitted AIR (`PastaMsmScalarDerive.WD`,
# `circuit/src/mina_opening_witness.rs:60`; it was 1,876 before the canonicity columns landed and
# this budget quoted the old width). The whole binary is ~365s at
# `--test-threads=2` in release; the single honest proof is ~41s. Segregating it into `heavy` would
# take four live teeth out of the everyday gauntlet, which is the `Documented ≠ Detected` failure.
# Run it as `cargo nextest run -p dregg-circuit --test pasta_derive_prove` (release strongly
# preferred; debug is dominated by the RCB witness generation).
#
# ⚑ `turn_anchor_binds_a_proof_to_the_committee` and `the_served_pair_binds_the_proofs_commitments`
# joined this row on 2026-08-07, BUDGETED rather than segregated — and the FIRST of them was
# already here in spirit and NOT in fact, which is why it was manufacturing false reds. Both mint a
# REAL full-turn STARK through `turn_proving::prove_and_verify_finalized_turn` and then drive the
# refusal through the real axum router: they are the two endpoint teeth (`expected_turn_hash` and
# `expected_old/new_commit`), so segregating them into `heavy` is the `Documented ≠ Detected`
# failure — the everyday gauntlet would stop checking that a stranger's re-verification can refuse
# anything at all.
#
# MEASURED 2026-08-07 on the laptop, `default` profile, debug, box under ~10 concurrent cargos:
# `the_served_pair_…` **158.4 s, PASS** — against the default profile's `45s × 4 = 180s` kill. That
# is 22 s of headroom, and its sibling `turn_anchor_…` **hit the cap and was TERMINATED at 180.0 s**
# on the same box in the same minute. Neither test regressed; the budget was simply below the work.
# A cap that a green test clears by 12% is not a cap, it is a coin flip, and the coin had already
# come up tails for the sibling. 120s × 30 applies to both now.
#
# `cap_open_write_prove_through` joined this row on 2026-07-28, BUDGETED rather than
# segregated into `heavy`: it is a security tooth (each test drives a real
# `prove_vm_descriptor2` roundtrip plus five red-proofs, and the two REMOVE members
# additionally prove that a FABRICATED post-remove cap-root is UNSAT), so it belongs in
# the everyday gauntlet. It was already minutes per test, and the tombstone after-spine
# added a second full prove attempt per REMOVE member; the file documents its own runtime
# in its header. Run it as `cargo nextest run -p dregg-circuit --test
# cap_open_write_prove_through -- --test-threads=4`.
#
# ⚑ FOUR `dregg-node` ROWS JOINED ON 2026-07-30, and the first of them was ALREADY A RED.
# Measured on hbox lane `privnode`, debug, `cargo nextest run -p dregg-node --no-fail-fast
# --test-threads=4`:
#
#   quorum_decision_matches_verified_lean_gate          TIMEOUT at 180.008s  (killed)
#                                                       -> PASS at 540.146s once budgeted, i.e. it
#                                                          is 3x the old kill and could never have
#                                                          passed under it. Still only 15% of the
#                                                          3600s granted here, and at
#                                                          --test-threads=4 it ran concurrently
#                                                          with the rest and did NOT set the
#                                                          package's 833s wall clock.
#   retained_history_ivc_seam (binary)                  PASS    at 172.897s  (7.1s of headroom)
#   retained_finalized_turn_round_trips_and_readmits    PASS    at 126.364s
#   n3_plateau_probe (binary)                           PASS    at 113.542s
#
# None of these was in ANY heavy clause, so all four ran in `default` against the 45s x 4 = 180s
# kill. The first was killed by it and reported as one of `dregg-node`'s failures; the second
# passed with 4% of the budget left, i.e. it is the same red one busy box away. That is precisely
# the "a kill budget that truncates real work manufactures a red that says nothing" failure this
# file's TIMEOUT DOCTRINE calls worse than having no budget at all.
#
# They are BUDGETED here rather than segregated into `heavy`, on the same reasoning the
# `cap_open_write_prove_through` and `pasta_derive_prove` rows above record.
# `quorum_decision_matches_verified_lean_gate` is a live DIFFERENTIAL TOOTH: ~60 tallies (11
# targeted edges + 48 deterministic pseudo-random cases) driven through BOTH the real
# `VoteCollector` — with real hybrid Ed25519+ML-DSA-65 signed votes, over the node's own
# Lean-verified ML-DSA cores — AND `dregg_lean_ffi::verified_finalization_quorum`, asserting the
# decided root agrees on every case. Moving it to `heavy` would take that tooth out of the
# everyday gauntlet, which is the `Documented != Detected` failure. It is slow because each case
# mints a fresh committee and signs every vote for real.
#
# ⚑ `exact_tuple_installs_and_crash_reopens_the_same_head` joined this BUDGET row on
# 2026-08-11 after the curator-pin persistence tooth made the genuine Signal
# initialization path explicit. Its release run reached nextest's 180.011s kill
# with no refusal or panic. It remains in the default set because it is the
# crash/reopen and exact-retry tooth for the operator ceremony; this override
# grants a verdict instead of manufacturing a timeout red.
#
# ⚑ `both_poles_through_the_deployed_prover` JOINED ON 2026-07-31, one TEST rather than its binary —
# the rest of `field_lanes9_canonicity_gate` is a registry census that runs in milliseconds and must
# keep its tight budget. This one drives FIVE full `prove_vm_descriptor2`/`verify_vm_descriptor2`
# roundtrips against the committed 1864-column `setFieldVmDescriptor2-3R24`: the honest 32-byte
# write at the gadget's only live arm (`q0 = q1 = 2`), the zeroed-aux red-proof, the forged nonet,
# and the stripped-descriptor control that makes the forged refusal mean CANONICITY. It is the only
# place either canonicity pole is asserted through a prover rather than over descriptor bytes, so
# segregating it into `heavy` would be the `Documented != Detected` failure — the completeness pole
# (an honest turn must not go UNSAT) is precisely the one nobody re-runs on demand.
slow-timeout = { period = "120s", terminate-after = 30 }

# The three HANG classes. Tight, because there is nothing here to protect: none of
# these has a legitimate slow path. 30s × 2 = 60s, well above their honest runtimes
# and far below "forever". DELETE THESE ROWS when the root causes land; do not retune.
[[profile.default.overrides]]
filter = 'package(dregg-zkoracle-prove) or package(dregg-zkoracle-live) or package(servo-render) or package(starbridge-tussle)'
slow-timeout = { period = "30s", terminate-after = 2 }

# ⚑ THE PROVING LEAF-ADAPTERS AND RING-CLEARING AIRs IN `dregg-circuit-prove`.
#
# MEASURED 2026-07-29, and the first measurement was WRONG in an instructive way. A lane reported
# these as load-dependent — "they pass uncontended and TIMEOUT under load". That was an INFERENCE
# stated as fact; not one of them had ever been OBSERVED to pass. Driven properly under bare
# `cargo test` with no nextest kill:
#
#     caveat_admission_leaf_adapter::tests::admission_binds_to_trade ... ok
#     test result: ok. 1 passed ... finished in 700.38s
#
# single-threaded, ~11 GB RSS, 99.9% CPU. It PASSES — in 700s, which is **3.9x** `[profile.default]`'s
# `45s x 4 = 180s`. So this is not contention and not a hang: it is a DETERMINISTIC class of
# real multi-minute proving tests that sit in the default profile with no budget and no `heavy`
# routing, and are therefore red in ANY `cargo nextest run -p dregg-circuit-prove`, on any box.
#
# That is exactly the failure this file's own TIMEOUT DOCTRINE names — "a budget that truncates a
# real 5-minute fold is exactly the failure this file calls worse than having no budget at all" —
# and the four suites above were granted budget for it. These are the ones that were missed.
#
# ⚠ CONSEQUENCE FOR ANY RED COUNT: the workspace's 182-failure measurement is PARTLY A BUDGET
# ARTIFACT. Nothing was broken in these; the budget was. Re-measure before attributing them.
#
# 720s x 2 = 24 minutes, chosen so the observed 700s worst case has headroom without becoming
# "never terminate" — this file's other stated bound, since an unbounded heavy suite is how
# `cargo test --workspace` came to need an external watchdog on 2026-07-26.
[[profile.default.overrides]]
# 2026-08-07: `shielded_ring_clearing_air`, `shielded_ring_clearing_nleg_air` and
# `shielded_spend_leaf_adapter` were dropped from this budget because the modules are DELETED
# (the Rust-authored shielded-spend tower, house law #1). A filter naming a test that cannot
# exist is a budget that can never be exercised — exactly the fail-open shape this file's
# timeout doctrine warns about — so the names go with the code.
#
# ⚠ AND THE NOTE ABOVE USED TO LIVE INSIDE THE FILTERSET BELOW, where `#` is NOT a comment —
# it is filterset syntax. nextest validates EVERY profile on EVERY invocation, so this broke
# `cargo nextest` REPO-WIDE at HEAD: any invocation, any package, exited before running a
# single test with `failed to parse profile.default … expected close parenthesis`. A lane
# measuring "0 tests failed" against that would have been reading a parse error as a green.
filter = '''package(dregg-circuit-prove) and (
  test(/caveat_admission_leaf_adapter/) or test(/blinded_membership_leaf_adapter/)
  or test(/solvency_leaf_adapter/)
  or test(/presentation_leaf_adapter/) or test(/factory_leaf_adapter/)
  or test(/hatchery_leaf_adapter/)
  or test(/custom_fold_wire_tests/) or test(/broken_order_rejected/)
)'''
slow-timeout = { period = "720s", terminate-after = 2 }

[profile.default]
# Default runs should execute backend coverage that is passing on the 24-core
# test host. Keep only the currently failing/timeout diagnostics quarantined,
# AND segregate the minute-scale recursion/IVC/fold/proptest suites (run them
# with `--profile heavy`). The heavy clause is the trailing `or binary(...) /
# or test(...)` group.
default-filter = 'not (test(~preflight_apps) or test(~preflight_effect_vm) or test(~preflight_proofs) or test(~preflight_turns) or test(~preflight_backends) or test(~preflight_composition) or test(~preflight_lean_marshal) or test(~preflight_node) or test(~preflight_sovereign) or test(~preflight_cli) or test(~preflight_demo_agent) or test(~preflight_golden_master) or binary(descriptor_leaf_recursion) or binary(rotation_batchstark_leaf_smoke) or binary(proptest_invariants) or binary(private_attested_clearing) or binary(private_book_bfv_zk) or binary(private_book_distributed_bfv_exact) or binary(private_bfv_attested_clearing) or binary(private_clearing_apex_e2e) or binary(poa_dark_bazaar_protocol) or binary(private_clearing_crown_consequence) or binary(private_clearing_guild_allocation) or binary(private_bazaar_playable_journey) or binary(energy_dispatch) or test(~k_fold_turn_chain_proves_and_verifies) or test(~two_step_inductive_core_proves_and_verifies) or test(~shielded_whole_note_swap_substrate_hiding_proof_and_hostile_publics) or test(~foreign_circuit_root_is_refused_by_vk_pin) or test(~authenticated_encrypted_orders_gate_real_game_asset_settlement) or test(~production_bfv_book_a_and_valid_hidingfri_book_b_cannot_cross_pair) or test(~private_bazaar_winner_spends_the_proof_assigned_mender_through_signed_spine_once) or test(~opening_check_proves_and_verifies_on_a_real_devnet_block_end_to_end) or test(~run_js_over_the_world_bridge_lands_on_the_served_world) or test(~pq_route_discovery_is_lazy_but_first_real_call_initializes) or test(~lean_init_lifecycle_heavy))'
# With the heavies gone, default tests are well under a minute. Flag stragglers
# at 45s; keep a generous kill backstop (45s × 4 = 180s) so legitimately-chunky
# integration tests are not falsely terminated.
slow-timeout = { period = "45s", terminate-after = 4 }

[profile.full]
# Run EVERYTHING (heavies included, no default-filter), be patient.
# Use: cargo nextest run --profile full   (consider --release for the folds)
# Custom profiles inherit `profile.default.default-filter` unless they replace
# it explicitly; `all()` is what makes this profile genuinely full.
default-filter = 'all()'
# 60s × 60 = 3600s. GENEROUS ON PURPOSE, and it used to be 600s. `full` is the only
# profile that runs the hour-scale folds AND the ordinary suites in one pass, so its
# GLOBAL number is the one an un-enumerated slow test falls back on — and the reference
# run has single tests averaging 516s (`ext_degree_cost_measure`) that appear in no
# heavy clause anywhere. A 600s global would have killed them. The tight bounds in this
# file are applied BY NAME to the three known hang classes (60s, in the override above),
# which is where a tight bound belongs; the global's job is only to guarantee that an
# UNKNOWN hang terminates eventually rather than never. An hour, once, is a cost worth
# paying to never truncate real work.
slow-timeout = { period = "60s", terminate-after = 60 }

[profile.ci]
# CI keeps the same passing-backend coverage as default (heavies segregated),
# but fail-fast. Mirror of profile.default's filter.
default-filter = 'not (test(~preflight_apps) or test(~preflight_effect_vm) or test(~preflight_proofs) or test(~preflight_turns) or test(~preflight_backends) or test(~preflight_composition) or test(~preflight_lean_marshal) or test(~preflight_node) or test(~preflight_sovereign) or test(~preflight_cli) or test(~preflight_demo_agent) or test(~preflight_golden_master) or binary(descriptor_leaf_recursion) or binary(rotation_batchstark_leaf_smoke) or binary(proptest_invariants) or binary(private_attested_clearing) or binary(private_book_bfv_zk) or binary(private_book_distributed_bfv_exact) or binary(private_bfv_attested_clearing) or binary(private_clearing_apex_e2e) or binary(poa_dark_bazaar_protocol) or binary(private_clearing_crown_consequence) or binary(private_clearing_guild_allocation) or binary(private_bazaar_playable_journey) or binary(energy_dispatch) or test(~k_fold_turn_chain_proves_and_verifies) or test(~two_step_inductive_core_proves_and_verifies) or test(~shielded_whole_note_swap_substrate_hiding_proof_and_hostile_publics) or test(~foreign_circuit_root_is_refused_by_vk_pin) or test(~authenticated_encrypted_orders_gate_real_game_asset_settlement) or test(~production_bfv_book_a_and_valid_hidingfri_book_b_cannot_cross_pair) or test(~private_bazaar_winner_spends_the_proof_assigned_mender_through_signed_spine_once) or test(~opening_check_proves_and_verifies_on_a_real_devnet_block_end_to_end) or test(~run_js_over_the_world_bridge_lands_on_the_served_world) or test(~pq_route_discovery_is_lazy_but_first_real_call_initializes) or test(~lean_init_lifecycle_heavy))'
slow-timeout = { period = "60s", terminate-after = 2 }
fail-fast = true

# ─────────────────────────────────────────────────────────────────────────────
# THE ARMED LANE — the `#[ignore]`d teeth, which nothing ran
# ─────────────────────────────────────────────────────────────────────────────
# `#[ignore]` in this tree means "minutes-long REAL recursion fold", not "broken".
# But nothing executed them: ci.yml runs `cargo test --workspace` (which never
# runs ignored tests) and no script passed `--ignored`. So all 8
# `circuit-prove/tests/*_binding_deployed_tooth.rs` — the DEPLOYED light-client
# binding teeth, each forging a specific witness and requiring the fold to REFUSE
# it — were dead in automation. Written, adversarial, correct, and never run.
#
# `armed` runs exactly the ignored set:
#   scripts/test-gauntlet.sh armed              (== nextest --profile armed --release
#                                                   --run-ignored ignored-only)
# ⚠ AND THAT IS A COMMAND A HUMAN TYPES, NOT COVERAGE. This paragraph used to end
# "and .github/workflows/armed-teeth.yml runs it on a schedule", which was FALSE —
# `scripts/test-gauntlet.sh` header records the measurement: that workflow runs its own
# hand-built `cargo test -p dregg-circuit-prove --release … -- --ignored` and has never
# invoked this profile or that script. The teeth DO run nightly, by that other route;
# `[profile.armed]` is not what runs them. Corrected 2026-07-27 — if you wire this
# profile into a workflow, correct this paragraph in the same commit.
#
# THE GPU EXCLUSION IS LOAD-BEARING, not tidiness. The GPU teeth fail CLOSED on a
# missing adapter (they assert `adapter_available()` — the crate's own correct
# pattern, now applied uniformly). On a GPU-less box they are therefore a
# guaranteed RED that says nothing about the code. They are `#[ignore]`d so a
# GPU-less runner skips them EXPLICITLY, and they run in `[profile.gpu]` on a box
# that has an adapter. Excluding them here is what lets the assert stay hard.
# Scoped to `package(dregg-circuit-prove)` on purpose. `--run-ignored ignored-only` across the
# WHOLE workspace would also sweep in `dregg-node`'s real-process soak tests (which spawn 4–5 node
# processes over QUIC) — a different lane with different prerequisites, not these teeth. The plan's
# target was `-p dregg-circuit-prove`; `package()` expresses that INSIDE the filter, because `-p`
# on the command line makes every OTHER profile's `binary()` names unresolvable and nextest then
# refuses to run at all.
#
# ⚑ FIVE GPU TEETH WERE IN NEITHER LIST (fixed 2026-08-08). Measured 2026-08-07 by
# `.github/ignored-test-routes.txt`'s census: `[profile.gpu]` named three binaries and six
# test names, this exclusion named the same nine, and these five appeared in neither —
#     gpu_lde_parity_apex_scale_dim0_fold                          (circuit-prove/src/gpu_backend.rs)
#     deployed_arities_are_bit_exact_against_pinned_plonky3        (gpu_hidingfri_fold_differential)
#     lean_ir2_hidingfri_proof_uses_gpu_merkle_and_is_cpu_exact    (gpu_hidingfri_ir2_e2e)
#     required_hidingfri_refuses_disabled_gpu_merkle_stage         (gpu_hidingfri_ir2_e2e)
#     deployed_kernel_step_cpu_vs_gpu_across_fri_parameterizations (gpu_kernel_step_reparam_measure)
# — so the GPU lane never ran them AND `armed` swept them onto whatever box typed the
# command. That is precisely the hazard the ⚠⚠ block at the top of this file names ("a
# renamed GPU tooth drops out of `gpu` and into `armed`, i.e. onto GPU-less boxes"),
# reached by never ENTERING either list rather than by a rename. All four clauses below
# are added to BOTH profiles in the same edit; splitting them is how this recurs.
#
# ⚠ AND `[profile.gpu]` ALONE WAS NOT ENOUGH TO ARM THEM. Three of the five open with
# `assert_eq!(std::env::var("DREGG_REQUIRE_WGPU").as_deref(), Ok("1"))` — they are
# meaningful only in the strict posture, and `scripts/test-gauntlet.sh gpu` did not set it,
# so listing them here without that would have armed three tests that cannot pass on their
# own lane. The script now exports it; see its `gpu)` row.
#
# Three are named by `binary(...)` on purpose: nextest HARD-ERRORS on an unresolvable
# `binary()` and is SILENT on an unresolvable `test(~)` (measured, see the top of this
# file), so the self-validating operator is used wherever the target permits it. Only
# `gpu_lde_parity_apex_scale_dim0_fold` needs `test(~)`, because it is a LIB test and no
# `binary()` names a lib target.
[profile.armed]
default-filter = 'package(dregg-circuit-prove) and not (binary(gpu_babybear_merkle_e2e) or binary(gpu_backend_shrink_e2e) or binary(gpu_recursion_fold_e2e) or binary(gpu_hidingfri_fold_differential) or binary(gpu_hidingfri_ir2_e2e) or binary(gpu_kernel_step_reparam_measure) or test(~gpu_dft_parity_vs_radix2) or test(~gpu_mmcs_root_parity_openings_and_reject) or test(~gpu_bn254_mmcs_wide_leaf_parity) or test(~gpu_lde_device_residency_hit_fallback_and_root_parity) or test(~gpu_babybear_lde_residency_hit_and_root_parity) or test(~gpu_lde_parity_apex_scale_dim0_fold) or test(~on_device_async_bn254_root_matches_cpu))'
# Real folds: warn late. A tooth killed at a timeout is a tooth that did not bite,
# so the warn period stays at five minutes and the kill is deliberately enormous —
# 300s × 12 = 3600s per tooth. This profile had NO `terminate-after` at all, which
# reads as respect for the folds but is not: `armed` is the NIGHTLY, and a nightly
# whose one deadlocked tooth never returns does not report a red, it reports NOTHING, and
# "no report" is the state this whole lane exists to abolish. An hour is longer than
# any fold ever measured here (the slowest recorded anywhere in this file is >1080s)
# by a factor of three, so this can only ever fire on a genuine hang.
slow-timeout = { period = "300s", terminate-after = 12 }
# CPU-bound + memory-heavy, same reasoning as [profile.heavy]. Override with -j.
test-threads = 4

# THE GPU LANE — the other half of the one GPU law.
# Every test here asserts a real adapter and is `#[ignore]`d. Run it ONLY on a box
# with a GPU: `scripts/test-gauntlet.sh gpu`. On a GPU-less host these SHOULD red —
# that is the fail-closed posture working, not a bug to route around.
#
# ⚠ THE LANE EXPORTS `DREGG_REQUIRE_WGPU=1` (`scripts/test-gauntlet.sh gpu`). The three
# HidingFRI/kernel-step teeth added below refuse to run without it — their first statement
# asserts it — and the variable is read at exactly four sites, all on the HidingFRI path
# (`gpu_backend.rs`: `enforce_required_gpu_hiding_boundary`, `GpuHidingFriFold::new`,
# `prove_vm_descriptor2_gpu_zk`, `require_hiding_gpu_dispatch_since`). None of the six
# original members touches any of them, so the export cannot change their verdict; checked
# file by file rather than assumed, because "the fallback test" is a name that invites the
# opposite guess (`gpu_lde_device_residency_hit_fallback_and_root_parity` reads no env at all).
[profile.gpu]
default-filter = 'binary(gpu_babybear_merkle_e2e) or binary(gpu_backend_shrink_e2e) or binary(gpu_recursion_fold_e2e) or binary(gpu_hidingfri_fold_differential) or binary(gpu_hidingfri_ir2_e2e) or binary(gpu_kernel_step_reparam_measure) or test(~gpu_dft_parity_vs_radix2) or test(~gpu_mmcs_root_parity_openings_and_reject) or test(~gpu_bn254_mmcs_wide_leaf_parity) or test(~gpu_lde_device_residency_hit_fallback_and_root_parity) or test(~gpu_babybear_lde_residency_hit_and_root_parity) or test(~gpu_lde_parity_apex_scale_dim0_fold) or test(~on_device_async_bn254_root_matches_cpu)'
# 300s × 4 = 1200s. A wedged GPU is a REAL failure mode here and it does not
# self-clear: a hung driver call blocks forever, and with `test-threads = 1` below
# it blocks the ENTIRE lane behind it, not just itself. Twenty minutes is far
# beyond any parity run and is the difference between "one GPU tooth reports
# TIMEOUT" and "the GPU lane produced no output overnight".
slow-timeout = { period = "300s", terminate-after = 4 }
# The GPU is a single shared device; serialize so parity runs do not contend.
test-threads = 1

[profile.heavy]
# The on-demand slow suite: ONLY the minute-scale recursion / IVC / fold /
# proptest tests that the default/ci profiles segregate out. Inverse of the
# heavy clause above.
#   cargo nextest run --profile heavy            (debug: minutes)
#   cargo nextest run --profile heavy --release  (RECOMMENDED — debug is the
#                                                 dominant slowness here)
# The kimchi/preflight quarantine still applies (don't resurrect known-failing
# diagnostics); we just flip the heavy set from excluded to *included-only*.
default-filter = '(binary(descriptor_leaf_recursion) or binary(rotation_batchstark_leaf_smoke) or binary(proptest_invariants) or binary(private_attested_clearing) or binary(private_book_bfv_zk) or binary(private_book_distributed_bfv_exact) or binary(private_bfv_attested_clearing) or binary(private_clearing_apex_e2e) or binary(poa_dark_bazaar_protocol) or binary(private_clearing_crown_consequence) or binary(private_clearing_guild_allocation) or binary(private_bazaar_playable_journey) or binary(energy_dispatch) or test(~k_fold_turn_chain_proves_and_verifies) or test(~two_step_inductive_core_proves_and_verifies) or test(~shielded_whole_note_swap_substrate_hiding_proof_and_hostile_publics) or test(~foreign_circuit_root_is_refused_by_vk_pin) or test(~authenticated_encrypted_orders_gate_real_game_asset_settlement) or test(~production_bfv_book_a_and_valid_hidingfri_book_b_cannot_cross_pair) or test(~private_bazaar_winner_spends_the_proof_assigned_mender_through_signed_spine_once) or test(~opening_check_proves_and_verifies_on_a_real_devnet_block_end_to_end) or test(~run_js_over_the_world_bridge_lands_on_the_served_world) or test(~pq_route_discovery_is_lazy_but_first_real_call_initializes) or test(~lean_init_lifecycle_heavy))'
# These run for minutes; warn at two minutes. The kill is 120s × 30 = 3600s — the
# same budget the named heavy-set override at the top of this file grants, restated
# here so the profile is honest read on its own. (In practice the override governs:
# this profile's `default-filter` selects exactly the set that override matches, and
# an inherited `[[profile.default.overrides]]` rule beats a custom profile's own
# profile-level `slow-timeout` — verified 2026-07-27.) What changed is that there IS
# a ceiling: "never auto-terminate" meant a deadlock in the heavy suite ran until a
# human intervened, which is precisely how `cargo test --workspace` came to need an
# external watchdog on 2026-07-26.
slow-timeout = { period = "120s", terminate-after = 30 }
# The folds are CPU-bound and memory-heavy; bound parallelism so they don't
# thrash. Override on the host with `-j` if you have the cores to spare.
test-threads = 4
