{
  "name": "drex-web-v2",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "DrEX product frontend (v2) — Preact + htm + signals, esbuild-bundled, self-contained.",
  "scripts": {
    "build": "node build.mjs",
    "serve": "node serve.mjs",
    "dev": "node build.mjs --watch & node serve.mjs"
  },
  "dependencies": {
    "preact": "^10.29.0",
    "@preact/signals": "^2.0.0",
    "htm": "^3.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
