// =============================================================================
// Section 16: Conclusion
// =============================================================================

#import "../defs.typ": lean
= Conclusion <sec-conclusion>

A turn is the exercise of an attenuable, proof-carrying token over owned state,
leaving a verifiable receipt. From that one sentence the system follows: four
substances under four disciplines, eight verbs as their structural rules,
authority as constructive knowledge produced under a non-forgeability law, one
predicate algebra at four polarities with two computed prices, and a receipt
*Q* that is witness, projection, fold, and light-client claim at once.

The guarantees are not assertions about an idealized protocol. Their
non-cryptographic cores are theorems about the Lean kernel the node runs,
pinned to three axioms; their deployment force is conditional on the explicit
eight-carrier floor and on the host-coverage seams in @sec-limitations. Within
that boundary, authority is non-amplifying, value conserves, receipts bind the
post-state, spends are fresh, and one verified aggregate attests the whole
history without re-execution
(#lean("RecursiveAggregation.light_client_verifies_whole_history")). The
claim is therefore falsifiable at named boundaries: a fooling history must
break the kernel-to-circuit correspondence, the proof-extraction carrier, or a
commitment assumption.

The same capability and the same check carry across a distance parameter and
down through the stack. A local microkernel object and a distributed cell are
one attenuable reference whose single-machine limit is the strong case, not the
degraded one; on seL4 the kernel's capability graph isolates the domains,
dregg's mediates the cells within, and the verified executor itself runs inside
its protection domain; in a database reads are SQL and writes are verified
turns; and on the desktop a window is a capability whose rendering's
non-interference and rehydration are kernel theorems. The same receipt
underwrites an application's contract, a game's move, and a cross-chain
settlement. The proof architecture holds all of this together as it evolves:
the circuit rotates under proof, and every finalized turn stays verifiable
across shapes, so the one root a stranger checks always covers the whole
history.

The result is one check for a stranger, together with a precise account of what
that check still assumes.
