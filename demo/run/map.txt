THE ROOM MAP + LIVE VALIDATION — the play + forge surfaces, driven
driven run · attested-dm /game/map + /game/validate · narratorKind: model:gemma2:2b
served at http://127.0.0.1:56210/vault  and  http://127.0.0.1:56210/forge
============================================================================

A room-map visualizer (inline SVG, no libs) draws the dungeon's room graph on both the play
surface (your position + visited rooms) and the forge (the shape of the world you authored,
a disconnected room flagged). Live validation lints the .dungeon source as you type — a
gutter marker + a panel — so you see problems BEFORE ▶ Play (which stays authoritative).

----------------------------------------------------------------------------
THE DRIVEN CHECKS
----------------------------------------------------------------------------
  MAP       /game/map → 10 rooms, 18 exits, 3 barred (e.g. "it needs the lantern")
  VALIDATE  clean → stage="clean" · syntax-broken → stage="parse" line 6 · live world UNTOUCHED
  VALIDATE  broken.dungeon → stage="validate" · 3 errors (line-pinned) · no session opened
  VAULT     SVG map: 10 nodes, 18 edges (3 barred) · current highlighted · visited 1→3
  FORGE     authored-world map: 4 nodes · live-lint: syntax-broken → gutter ●error + stage="parse"; fixed → clean

============================================================================
The map is an aid; the service is the authority. /game/validate opens no session, changes
no state — ▶ Play remains the fail-closed compile that mounts a world.
