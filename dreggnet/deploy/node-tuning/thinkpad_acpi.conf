# node-a: always load thinkpad_acpi with fan control enabled
# so /proc/acpi/ibm/fan accepts "level <n>" after every boot.
# Install to: /etc/modprobe.d/thinkpad_acpi.conf
options thinkpad_acpi fan_control=1
